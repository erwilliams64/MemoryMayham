# problem: In the game of Memory, it always felt like an advantage to go second.If you were to play perfectly, what is the minimum board size so it would be advantageous to go first?

def makePlay(playerNumber,piecesRemaining, totalRevealed, turnNumber)
    # Random Probablility Match
    # Calc Magic
    # output Stats

    if piecesRemaining > 0 {
        makePlay(Outputstats)
    }